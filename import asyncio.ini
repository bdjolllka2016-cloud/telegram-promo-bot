import asyncio
from aiogram import Bot, Dispatcher, types
from aiogram.utils import executor
import sqlite3
from datetime import datetime

TOKEN = "8442006569:AAEH03mtwRrRj0-fkFXXm7j73AXmoGK1VD4"
ADMIN_ID = 5597660360  # замените на свой Telegram user_id

bot = Bot(token=TOKEN, parse_mode="HTML")
dp = Dispatcher(bot)

# --- база данных ---
conn = sqlite3.connect("data/bot.db")
cursor = conn.cursor()

cursor.execute("""
CREATE TABLE IF NOT EXISTS users (
    user_id INTEGER PRIMARY KEY,
    username TEXT,
    subscribed_at TEXT
)
""")
cursor.execute("""
CREATE TABLE IF NOT EXISTS answers (
    user_id INTEGER PRIMARY KEY,
    q1 TEXT,
    q2 TEXT,
    q3 TEXT,
    q4 TEXT
)
""")
cursor.execute("""
CREATE TABLE IF NOT EXISTS promocodes (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    code TEXT,
    description TEXT,
    discount INTEGER,
    start_date TEXT,
    end_date TEXT,
    active INTEGER DEFAULT 1
)
""")
conn.commit()

# --- шаги опроса ---
user_steps = {}

@dp.message_handler(commands=['start'])
async def cmd_start(message: types.Message):
    user_id = message.from_user.id
    username = message.from_user.username or ""
    now = datetime.utcnow().isoformat()

    cursor.execute("INSERT OR IGNORE INTO users (user_id, username, subscribed_at) VALUES (?, ?, ?)",
                   (user_id, username, now))
    conn.commit()

    user_steps[user_id] = {"step": 1, "answers": {}}
    await message.answer("👋 Привет! Ответьте, пожалуйста, на несколько вопросов.\n\n1️⃣ Сколько рулонов Вы планируете приобрести?")

@dp.message_handler()
async def handle_answers(message: types.Message):
    user_id = message.from_user.id
    if user_id not in user_steps:
        return

    step = user_steps[user_id]["step"]

    if step == 1:
        user_steps[user_id]["answers"]["q1"] = message.text
        user_steps[user_id]["step"] = 2
        await message.answer("2️⃣ В какой город необходима доставка?")

    elif step == 2:
        user_steps[user_id]["answers"]["q2"] = message.text
        user_steps[user_id]["step"] = 3
        kb = types.ReplyKeyboardMarkup(resize_keyboard=True)
        kb.add("Да", "Нет")
        await message.answer("3️⃣ Нужна ли консультация?", reply_markup=kb)

    elif step == 3:
        user_steps[user_id]["answers"]["q3"] = message.text
        user_steps[user_id]["step"] = 4
        await message.answer("4️⃣ Если выбрали артикул – напишите его. Отправим запрос на склад.", reply_markup=types.ReplyKeyboardRemove())

    elif step == 4:
        user_steps[user_id]["answers"]["q4"] = message.text

        # Сохраняем ответы
        ans = user_steps[user_id]["answers"]
        cursor.execute("INSERT OR REPLACE INTO answers (user_id, q1, q2, q3, q4) VALUES (?, ?, ?, ?, ?)",
                       (user_id, ans["q1"], ans["q2"], ans["q3"], ans["q4"]))
        conn.commit()

        del user_steps[user_id]

        # Проверяем промокод
        cursor.execute("SELECT code, description, discount, end_date FROM promocodes WHERE active = 1 ORDER BY id DESC LIMIT 1")
        promo = cursor.fetchone()

        if promo:
            code, description, discount, end_date = promo
            await message.answer(
                f"🎁 Спасибо за ответы!\n\nВаш промокод: <b>{code}</b>\n{description}\nСкидка: <b>{discount}%</b>\nДействует до: {end_date}"
            )
        else:
            await message.answer("Спасибо за ответы! Пока нет активных промокодов.")

# --- админские команды ---

@dp.message_handler(commands=['newcode'])
async def cmd_newcode(message: types.Message):
    if message.from_user.id != ADMIN_ID:
        return

    parts = message.text.split(" ", 1)
    if len(parts) < 2:
        await message.answer("Использование: /newcode код;описание;скидка;датазакинчания(YYYY-MM-DD)")
        return
    try:
        code, description, discount, end_date = [x.strip() for x in parts[1].split(";")]
        discount = int(discount)
        start_date = datetime.utcnow().date().isoformat()

cursor.execute("INSERT INTO promocodes (code, description, discount, start_date, end_date, active) VALUES (?, ?, ?, ?, ?, 1)",
                       (code, description, discount, start_date, end_date))
        conn.commit()

        await message.answer("✅ Новый промокод добавлен и активирован.")

        cursor.execute("SELECT user_id FROM users")
        users = cursor.fetchall()
        for (uid,) in users:
            try:
                await bot.send_message(uid, f"🎉 Новый промокод: <b>{code}</b>\n{description}\nСкидка: {discount}%\nДействует до: {end_date}")
            except:
                pass

    except:
        await message.answer("❌ Ошибка в формате.")

@dp.message_handler(commands=['endcode'])
async def cmd_endcode(message: types.Message):
    if message.from_user.id != ADMIN_ID:
        return
    try:
        promo_id = int(message.text.split(" ")[1])
        cursor.execute("UPDATE promocodes SET active = 0 WHERE id = ?", (promo_id,))
        conn.commit()
        await message.answer(f"Промокод {promo_id} деактивирован.")
    except:
        await message.answer("❌ Ошибка: неверный ID.")

if name == "main":
    executor.start_polling(dp, skip_updates=True)